# Demo Mode Configuration for GitHub Pages
# This file is used for deploying the app as a static site

# Enable demo mode (uses localStorage instead of database, client-side LLM)
NEXT_PUBLIC_DEMO_MODE=true

# Base path - update this if deploying to a GitHub Pages repo subdirectory
# Example: for https://username.github.io/repo-name/, use NEXT_PUBLIC_BASE_PATH=/repo-name
NEXT_PUBLIC_BASE_PATH=
